import React from 'react';
import { Typography } from '@mui/material';

const Revision = () => {
  return (
    <div>
      <Typography variant="h4">Revision</Typography>
      <Typography variant="body1">This is the Revision page.</Typography>
    </div>
  );
};

export default Revision;
