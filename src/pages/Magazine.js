import React from 'react';
import { Typography } from '@mui/material';

const Magazine = () => {
  return (
    <div>
      <Typography variant="h4">Magazine</Typography>
      <Typography variant="body1">This is the Magazine page (Entree/Sortie).</Typography>
    </div>
  );
};

export default Magazine;
